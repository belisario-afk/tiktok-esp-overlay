{
  "build": {
    "env": {
      "TIKTOK_USERNAME": {
        "required": true,
        "default": "lmohss"
      },
      "PORT": {
        "required": false,
        "default": "8080"
      }
    },
    "buildCommand": "npm install",
    "startCommand": "npm start"
  }
}